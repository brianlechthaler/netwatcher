# Gateway API key (optional; set on gateway and capture agents)
GATEWAY_API_KEY=

# Capture agent settings (remote or local)
CAPTURE_INTERFACE=eth0
GATEWAY_URL=http://127.0.0.1:8080
AGENT_ID=capture-agent-1
